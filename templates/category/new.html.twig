{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Create Category{% endblock %}

{% block body %}
    <h1 style="margin-top: 2%; margin-bottom: 3%; margin-left: 22%">Create new Category</h1>
    <div class="container">
        <div class="col-md-8">
            {{ include('category/_form.html.twig') }}
        </div>
        <button class="btn btn-primary" style="margin-left: 1.3%; margin-top: 3%"><a style="color: white" href="{{ path('category_index') }}">Back to list</a></button>
    </div>
{% endblock %}
